# Override nested postcss vulnerability — our own postcss is up-to-date
# The nested version comes from Next.js internals and is not exploitable
# in this context (no server-rendered CSS interpolation of user data).
overrides.postcss=^8.5.10
